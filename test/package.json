{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "server": "sirv --dev --no-clear --port 3000",
    "start": "npm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "sirv-cli": "^2.0.2"
  }
}
